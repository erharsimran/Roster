DATABASE_URL="postgresql://postgres:postgres@localhost:5432/roster?schema=public"
REDIS_URL="redis://localhost:6379"   # Valkey speaks the same protocol — ioredis needs no changes
JWT_SECRET="replace-with-a-real-secret"
PORT=3000


GOOGLE_CLIENT_ID="replace-with-a-real-id"
GOOGLE_CLIENT_SECRET="replace-with-a-real-id"
GOOGLE_CALLBACK_URL="replace-with-a-real-URL"

REDIS_HOST="localhost"
REDIS_PORT=6379